{
  "name": "alvesbandeira-game-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run clean && cd apps/web && npm run build",
    "preview": "cd apps/web && npm run preview",
    "clean": "node scripts/prebuild-clean.mjs"
  },
  "dependencies": {
    "@getbrevo/brevo": "^2.1.0",
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.5.0",
    "rate-limiter-flexible": "^5.0.4"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
